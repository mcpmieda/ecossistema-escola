# infra/factory

Provider-neutral Factory Control Plane foundation.

- `control-plane.mjs`: validates and materializes Factory Run issues idempotently.
- `examples/`: safe fixtures used by CI.

External AI providers are intentionally not dispatched in this phase.
