name: Temporary normalize Drawer state - retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    name: Retired temporary helper
    runs-on: ubuntu-latest
    steps:
      - name: No operation
        run: echo "Temporary normalization already applied; this helper is retired."
