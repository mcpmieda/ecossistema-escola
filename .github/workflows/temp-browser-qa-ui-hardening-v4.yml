name: Temporary browser QA UI hardening v4 - retired

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: temporary-ui-hardening-v4-retired
  cancel-in-progress: true

jobs:
  retired:
    name: Retired QA harness
    runs-on: ubuntu-latest
    steps:
      - name: No operation
        run: echo "Replaced by final QA harness."
