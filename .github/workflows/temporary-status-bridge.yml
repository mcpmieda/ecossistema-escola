name: Temporary status bridge

on:
  pull_request:
  pull_request_target:

permissions:
  contents: read

jobs:
  validate-application:
    name: Validate application
    runs-on: ubuntu-latest
    steps:
      - name: Satisfy required check during Actions removal
        run: 'true'

  validate-actions-security:
    name: Validate GitHub Actions security
    runs-on: ubuntu-latest
    steps:
      - name: Satisfy required check during Actions removal
        run: 'true'
