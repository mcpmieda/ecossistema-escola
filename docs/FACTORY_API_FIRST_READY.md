# Factory API-first readiness

This branch contains the API-first Jules orchestration candidate. Final activation requires CI approval, merge into the trusted branch, and configuration of `JULES_API_KEY` as a GitHub Actions secret.
